generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Skill {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  image       String
  skill       String
  description String
}
